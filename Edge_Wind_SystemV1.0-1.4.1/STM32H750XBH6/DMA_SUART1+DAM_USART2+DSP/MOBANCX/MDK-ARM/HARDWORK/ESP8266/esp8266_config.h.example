#ifndef __ESP8266_CONFIG_H
#define __ESP8266_CONFIG_H

/**
 * ESP8266 本地配置（示例）
 *
 * 使用方法：
 * 1) 复制本文件为同目录 `esp8266_config.h`
 * 2) 填写你的 WiFi / 服务器信息
 *
 * 注意：
 * - `esp8266_config.h` 已在项目 `.gitignore` 中忽略，不会被提交到 GitHub。
 */

#define WIFI_SSID       "YOUR_WIFI_SSID"
#define WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"

// Flask 后端所在 PC/服务器的局域网 IP（示例）
#define SERVER_IP       "192.168.1.100"
#define SERVER_PORT     5000

// 节点标识（可选，也可以继续使用 esp8266.h 中默认值）
// #define NODE_ID      "STM32_H7_Node"
// #define NODE_LOCATION "Lab_Test"

#endif /* __ESP8266_CONFIG_H */

